version: 1
frontend:
  phases:
    preBuild:
      commands:
        - NODE_ENV=development npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
